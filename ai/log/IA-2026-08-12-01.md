# IA-2026-08-12-01: resolución de PI-06 y ampliación de alcance de PI-08

**Fecha:** 2026-08-12
**Herramienta/modelo:** Claude Code (Claude Sonnet 5)
**Responsable:** Juan Pablo Valderrama Pino (VladPhil92)
**Archivos afectados:** `research/questions.md`, `PROJECT.md`, `README.md`,
`research/methodology.md`, `MASTER_EXECUTION_PLAN.md`,
`research/reading-schedule.md`, `research/sources/library-manifest.md`,
`governance/decision-log.md`
**Identificadores afectados:** `PI-06`, `PI-08`, `DEC-024`, `DEC-025`,
`SRC-219`–`SRC-226`

## Propósito y contexto

El investigador pidió resolver la decisión pendiente sobre la rúbrica de
`PI-06`, marcada `DECISIÓN HUMANA REQUERIDA` en `PROJECT.md` desde el
refinamiento de título de `DEC-010`. Al responder, el investigador añadió
una razón no prevista en la nota original (evitar «interespecie» por no ser
vocabulario derridiano) y, en el mismo intercambio, planteó que el problema
de la máquina/IA —frontera humano/IA, autonomía/singularidad, control
bélico, manipulación de mercados, regulación humana de la IA, dominación de
la IA sobre lo humano (control biopolítico), y si hay «algo más allá de lo
biopolítico»— había adquirido relevancia comparable a la de la animalidad y
que `PI-08` (`DEC-023`) se quedaba corta para cubrirlo.

## Entrada

Se usó `AskUserQuestion` dos veces, siguiendo `DEC-021` (la IA formaliza y
presenta el texto para aprobación explícita antes de escribirlo, nunca
decide la sustancia):

1. Primera ronda: (a) confirmar el texto exacto de la nueva rúbrica de
   `PI-06` entre el ya sugerido por `PROJECT.md` («Pertenencia más allá de
   la excepcionalidad humana») u otro propuesto por el investigador; (b)
   clasificar la reflexión sobre IA/máquina entre «no formalizar todavía»,
   «ampliación de `PI-08`» o «pregunta nueva (`PI-09`)».
2. Segunda ronda, tras conocer que se trataba de una ampliación de `PI-08`:
   se presentó un borrador completo de pregunta ampliada y nota de alcance
   para `PI-08`, y se preguntó (a) si aprobaba ese texto tal cual o quería
   reformularlo, y (b) si además de la pregunta quería ya una primera lista
   de candidatas bibliográficas para el alcance ampliado.

## Salida utilizada

- `DEC-024`: rúbrica de `PI-06` → «Pertenencia más allá de la excepcionalidad
  humana» (aprobada tal cual, texto ya propuesto).
- `DEC-025`: `PI-08` ampliada con la pregunta y la nota de alcance
  propuestas por esta sesión (aprobadas tal cual), más ocho candidatos
  nuevos (`SRC-219`–`SRC-226`: Bostrom, Crawford, Pasquale ×2, P. W. Singer,
  Kello, Hui, Floridi) propuestos de memoria por esta sesión, sin búsqueda
  externa de verificación, marcados `CANDIDATE`.
- Se sincronizaron los conteos de candidatas (219 → 227) en
  `research/methodology.md`, `README.md` y `MASTER_EXECUTION_PLAN.md`, y se
  anotó explícitamente en `research/reading-schedule.md` y `README.md` que
  ese cronograma (generado el 2026-08-11, antes de esta decisión) no
  incluye todavía las ocho candidatas nuevas.
- `research/dashboard.md` regenerado mecánicamente
  (`python3 scripts/generar_dashboard.py`).

## Verificación humana y fuentes de contraste

Los ocho candidatos de `SRC-219`–`SRC-226` **no** fueron verificados por
búsqueda web en esta sesión — se reportan de memoria, con el mismo aviso
honesto que recibió el primer lote de `PI-08` (`SRC-211`–`218`) antes de su
verificación en `DEC-023`/PR posterior. Autor, título, editorial y año
requieren la misma verificación por búsqueda web antes de avanzar de
`CANDIDATE` a `IDENTITY_VERIFIED`. Ninguna decisión de sustancia (texto de
la rúbrica de `PI-06`, texto de la pregunta y alcance de `PI-08`, elección
entre las tres opciones de tratamiento de la reflexión sobre IA) fue tomada
por la IA: las tres provienen de respuestas explícitas del investigador a
las preguntas planteadas por `AskUserQuestion`.

## Decisión humana

- [x] `ACEPTADA` — la salida (textos de `PI-06`, `PI-08` y la lista de ocho
      candidatas) se incorporó tal como se propuso, sin modificación
      sustantiva, por aprobación explícita del investigador en ambas rondas
      de preguntas.

## Cambios, descartes y comentarios del investigador

El investigador explicitó que la ampliación de `PI-08` no debe leerse como
reapertura del «doble frente» animal/IA ya superado (`DEC-003`): el animal
sigue siendo el único campo crítico privilegiado de la tesis (`CLAUDE.md`);
la frontera humano/IA es un campo de prueba adicional y paralelo, no un
segundo objeto que compita por esa posición. Queda explícitamente sin
resolver, por diseño, si el control biopolítico agota el problema de la
IA o si hay «algo más allá de lo biopolítico» — la nota de alcance de
`PI-08` lo deja como pregunta abierta a investigar, no como premisa.
