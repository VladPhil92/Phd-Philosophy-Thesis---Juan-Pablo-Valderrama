# Dissertation Relevance Index

Record `source_id → note_id → concept_id → argument_id → chapter_id → claim` links here only after researcher approval. Candidate AI links remain `INTERPRETATION_PENDING`.
