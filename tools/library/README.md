# Local Library Tooling

Reserved for small, auditable helpers under `ocr/`, `cleanup/`, `validation/`, and `indexing/`. Tools must preserve page markers/raw OCR, emit logs, never silently repair terms, and never convert a derivative into citation authority. Generated/private output belongs in ignored `tools/library/output/`. No OCR application is implemented because no source was supplied.
